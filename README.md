**LIVE LINK**
https://saas-landing-tau-smoky.vercel.app/

**For the live preview**
1. Sigup with the required informations
2. Or use "hello@world" and "Hello@123" as Login credentials

**SETUP**

1. Creted the app using React Vite
2. Install all the required packages (can check from package.json)
3. Create the Landing Page
4. Create the Server, then connect the MongoDB Database and JWT authentication code for Backend
5. Create an .env file and keep all the secret keys here
6. Create APIs for Login, Signup and Logout
7. Create the Login and Sigup page for Frontend
8. Integrated the Backend for login and signup pages
9. Crerate Dashboard, Users and Setting page

**FEATURES CHECKLIST**

1. Homepage = pixel-close Figma landing page
2. Auth + Dashboard = candidate-designed, but must be clean and usable
3. Dashboard must integrate with API and implement real UI behaviors
4. Used React vite and Tailwind CSS in tech stack
5. Not copied any existing SaaS template/dashboard theme
6. Not created any Drag-drop builders
7. Designed only the homepage frame from the design
8. Homepage follows the provided Figma design:
    layout, spacing, typography, section order
    button styles, cards, icons, etc.
9. Interactions: hover + focus states
10. Creted Login and Signup pages
11. Stored token in localStorage, protected dashboard routes and Logout clears token
12. Created Dashboard (Summary), Users (Core) and Settings page
13. Used this API for "https://jsonplaceholder.typicode.com/users" for Users
14. Users Page includes, List view (table or cards), Search (name/email), Sort (A–Z / Z–A),User detail (modal or route) and Pagination (client-side)
15. Dashboard summary shows, Total users count and Loading + error states handled properly
16. Settings page shows Basic profile form + theme toggle (light/dark) and Persist settings in localStorage
17. Project Quality Requirements-
    Clean component structure (Button, Input, Modal, Layout, etc.)
    Reusable UI primitives
    Proper empty/loading/error states
    No horizontal overflow on mobile
    Accessible basics:
    semantic elements
    keyboard usable
    visible focus states

**SCREENSHOTS**



**DECISIONS AND TRADEOFFS**

1. Decision - I have used JWT Authentication instead of using Fake Authentication and used React vite instead of Next.js as it takes much longer time than React vite to start the application
2. Tradeoffs - Signup or Login can take some time to render to the Dashboard



